/**
 * Classes that represent database configurations that are used by the system to connect to database servers.
 * These classes map directly to the XML file structure of the 'cfg_database_layer.xml' configuration file.
 */
package dev.mathops.persistence.config;
